<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w -full min-h-screen bg-slate-800 text-white p-10">
        <div class="flex  justify-end w-full ">
        <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block " href="/logout">LOGOUT</a></div>
        <a href="/read" class="text-2xl mt-4 ">Read all User</a>
        <h3 class="text-3xl mb-5">
            Hello, <%= user.name %>😎
        </h3>
        <h5 class="mb-5 ">You can create your own Quote</h5>
   <form action="/post" method="post">
    <textarea name="content" placeholder="what's your note" class=" p-3 resize-none w-1/3 bg-transparent border-2 border-zinc-700 rounded-md" id=""></textarea>
<input type="submit" value="create a post" class="block px-3 py-2 bg-blue-500 rounded-lg mt-2 w-50">   
</form>
<div class="post mt-20">
    <h3 class="text-2xl text-white">Your Quotes..</h3>
    <div class="postcontainer mt-5">
<% user.posts.reverse().forEach((ele)=>{%>
    <div class="post w-1/3 p-5 mb-4 border-2 mt-3 bg-slate-800 rounded-lg border-zinc-700">
        <h4 class="text-white text-3xl"><%= user.username%></h4>
        <p class="text-white tracking-tight"><%= ele.content%></p>
        <small class="inline-block mt-1"><%= ele.likes.length%>likes</small>
        <div class="btns flex gap-4 mt-5">
         <a href="/like/<%= ele._id%>"  class="px-3 py-2 bg-pink-500 rounded-lg" >
            <%= ele.likes.indexOf(user._id)? 'likes' : 'unlike'%>

         </a>
         <a href="/edit/<%= ele._id%>" class="px-3 py-2 bg-orange-500 rounded-lg ">edit</a>
        </div>
             </div>
             <a class=" px-3 py-2 bg-blue-500 rounded-lg mt-4  " href="/delete/<%= ele._id %>">delete the note</a>
            
<%})%>
<a class=" px-3 py-2 bg-blue-500 rounded-lg mt-2  " href="/logout">LOGOUT your Acut.</a>
    </div>
</div>
    </div>
   
</body>
</html>